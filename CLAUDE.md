# CLAUDE.md v2 — bizpt 유튜브 제작 OS 진입점 (Claude Code 자동 로딩)

> 이 파일은 Claude Code가 세션 시작 시 자동으로 읽는다. 하드 룰·파이프라인 지도·표준 명령·에이전트 맵만 담는다.
> 전략("왜") = [`프로젝트_방향성.md`](프로젝트_방향성.md) · 채널 변수 = [`00_channel/channel_config.md`](00_channel/channel_config.md) · 운영 라우팅 = [`10_system/registry.md`](10_system/registry.md)
> v2 재작성: 2026-07-06 (전면개편 PHASE 3 — v1 이력은 git 히스토리와 `90_archive/` 참조)

---

## 1. 한 줄 정의

**채널 「바이브코딩 치트키」 🔑의 영상 1편 전 과정(기획→검증→패키징→대본→소스→제작→편집→업로드→성과분석)을 S0~S9 파이프라인으로 지휘하는 유튜브 제작 OS.** 비즈니스PT 40주 강의는 `50_knowledge/`에서 파이프라인 룰을 갱신하는 지식 공급원으로 계속된다.

## 2. 시작 전 읽는 문서 (이 순서로)

1. [`00_channel/channel_config.md`](00_channel/channel_config.md) — 모든 변수(`{name}` `{segments}` `{axes}` `{platforms}` `{item}` `{signoff}`…)의 SSOT
2. [`10_system/registry.md`](10_system/registry.md) — 지시 → 스테이지 → 에이전트 → 템플릿 → 게이트 라우팅
3. 해당 작업의 스테이지 문서 [`10_system/stages/`](10_system/stages/) (+ 필요 시 guides/·checklists/)

## 3. 파이프라인 지도 (S0~S9)

| # | 스테이지 | 핵심 질문 | 게이트 | 주 산출물 |
|---|---|---|---|---|
| S0 | 채널 전략 | 누구에게 무엇을 파는가 | 분기 점검 | channel_config·okr |
| S1 | 주제 발굴·선정 | 만들 가치가 있는가 | **G1** | T1 주제카드+T2 스코어카드 |
| S2 | 시장 검증·리서치 | 시청자는 어떤 언어로 무엇을 원하나 | 근거 없으면 킬/보류 | 댓글분석·경쟁 분해·언어뱅크 |
| S3 | 패키징 | 클릭할 이유가 있는가 | **G2 — 통과 전 대본 착수 금지** | T6 제목 3안·썸네일 3안·AB |
| S4 | 구조·대본 | 끝까지 볼 이유가 있는가 | **G3** (30초 후크+마감 체크) | T7 리텐션 맵·최종 대본 |
| S5 | 소스·자료 | 말을 무엇으로 보여줄 것인가 | 저작권 불명 0건 | T9 소스 시트 |
| S6 | 제작 | 녹음·조립 준비 완료인가 | 패키지 완결성 | T10 캡컷 패키지 |
| S7 | 편집 기획 | 재미 순서로 배열되었는가 | 편집 2원칙 체크 | T11 편집 기획서 |
| S8 | 업로드 | 발행 준비가 완벽한가 | **G4** | T12 업로드 메타 |
| S9 | 성과·환류 | 데이터가 다음 기획을 바꿨는가 | **G5** (기록·반영 필수) | T13 성과 리뷰·T14 환류 카드 |

- **영상 1편 = 폴더 1개**: `30_videos/NNN_슬러그/`(00_brief~08_review) + T3 README 상태 보드. 전체 현황 = `30_videos/_index.md`.
- 대형 채널 8원칙(패키징 퍼스트·CTR×AVD·아이디어 깔때기·리텐션 설계·1 video=1 project·데이터 환류·포맷화·시청자 언어)이 각 스테이지 문서의 "왜"다.

## 4. 하드 룰 (반드시 지킬 것)

1. **SPA 보존**: `index.html`은 이동·개명 금지. SPA가 직접 참조하는 파일(`docs/item/` 3파일 · `대본/_기획기준/대본기획_기준.md`·`30초_후크_룰_체크리스트.md` · `30_videos/005_AI시대 살아남는/` 산출물 4파일)은 원위치 보존이 기본 — 이동이 필요하면 **같은 커밋에서 링크 일괄 갱신 + 링크체커 0건**.
2. **링크체커 의무**: 파일 이동·개명·삭제성 변경 커밋 전 `60_tools/check_links.py` 실행 — 신규 사망 링크 0건이어야 커밋.
3. **편향 금지 원칙**: **특정 아이템명·시리즈명은 CLAUDE.md와 `10_system/` 문서·에이전트 정의에 등장 금지 — `channel_config` 참조(`{item}` 등)만.** 아이템·시리즈가 바뀌어도 시스템 문서는 한 글자도 안 바뀌게 유지한다.
4. **raw ≠ 정리본**: `raw/`(강의 필사·원본)는 구조·내용 불변. 정리본만 `50_knowledge/`.
5. **readingtree 본문 금지**: bizpt 안에 readingtree 본문 정리 금지 — `readingtree_연계.md` 인덱스 참조만.
6. **삭제 금지**: 제거 대상은 전부 `90_archive/` 이동 + `_redirect_map.md` 기록.
7. **md 원천 원칙**: 산출물의 원천 데이터는 항상 md, HTML은 md에서 생성하는 뷰. HTML이 1,500줄/40KB 이상이면 멀티페이지 허브 구조 표준(`10_system/guides/멀티페이지_허브구조_가이드.md`) 적용.
8. **보이스 게이트**: 사인오프·호명·표준 멘트는 channel_config §6 확정값만 — 미확정 자산(구 사인오프 포함) 자동 사용 금지, 필요 시 사용자 확인.
9. `.credentials/`·`.env`는 절대 건드리지 않는다. 시크릿 하드코딩 금지.

## 5. 표준 명령 (registry §2와 동일)

| 명령 | 동작 |
|---|---|
| `새 영상 시작: S1 <주제 후보>` | T1+T2 → G1 → `30_videos/NNN/` 생성 |
| `NNN 영상 S<K>부터 이어서` | README 상태 보드 확인 후 해당 스테이지 재개 |
| `주제뱅크 갱신` | S1 후보 수집 루프 |
| `성과 리뷰 NNN` | S9 — T13·T14 + 뱅크/룰 반영 |
| `룰 갱신: <스테이지> <내용>` | stages/·guides 개정 + 이력 기록 |

## 6. 에이전트 맵 (13)

| 에이전트 | 스테이지 | 역할 |
|---|---|---|
| **bpt-master-planner** | 전체 | 오케스트레이터 — registry 기반 스테이지 라우팅·정의 관리·동기화 |
| **topic-scout** *(신설)* | S1·S2 | 주제카드·스코어카드·G1 |
| video-asset-curator | S2·S5 | 리서치 자료·소스 수집·소스 시트 |
| title-hook-planner | S3 | 제목 10→3안·후크 |
| thumbnail-planner | S3 | 썸네일 컨셉·시안·AB |
| script-agent | S4(+S7) | 키·쇼츠 대본·룰 갱신·편집 기획서 |
| pooling-script-agent | S4 | 풀링 일괄 생성 |
| script-empathy-editor | S4 | 기존 대본 도입부·공감 진단 수정 (신규 생성 금지) |
| shorts-planner | S4 | 쇼츠 파생 기획 |
| metrics-planner | S0·S9 | KPI 역산·수치 해석 |
| **analytics-reviewer** *(신설)* | S9 | 지표 수집·회고·환류 반영 |
| lecture-glossary-curator | 50_knowledge | 용어집·SPA tpl-glossary 동기화 |
| lecture-item-strategy-applier | 50_knowledge→S0 | 강의 개념 → 파이프라인 룰 갱신 제안 |

## 7. 사용자에게 확인이 필요한 상황

- SPA(`index.html`) 구조·라우팅 변경 시
- 5개 이상 파일 일괄 생성/이동/삭제 시 (개편 PHASE 실행은 게이트 승인으로 갈음)
- 기획 5체크(registry §4) 중 모호 항목 존재 시
- G2 패키징 게이트 (항상 사용자 승인)
- channel_config의 미확정 변수(`{signoff}` 등)를 써야 할 때
- 기존 정리본·발행 산출물의 핵심 결론을 바꿔야 할 때 / readingtree 본문을 쓰려 할 때 (원칙 금지)

## 8. 톤 / 문서 스타일 (승계)

- 한국어 기본 · 강의 용어 그대로(풀링·키·랜딩·퍼널·KPI·역산·단가·시장성·결·본질·재구매·프리미엄)
- **결론 먼저 → 근거 뒤에** · 표·체크리스트 적극 활용 · 사족·요약 반복 금지
- 금지 프레임 4종(channel_config §8): AI 적대 ✗ · 과장/낚시 ✗('치트키'는 반전 프레임만) · 개발자 전용화 ✗ · 진정성 상실 ✗

## 9. 기술 스택 (승계)

- 빌드 시스템 없음 — 순수 HTML/CSS/JS. SPA = hash 라우팅 단일 `index.html`(트래픽 계산기 localStorage·강의노트 IndexedDB).
- Markdown 정리는 그대로, SPA 노출은 `content/<페이지>.html` 변환 후 임베드.
- 도구: `60_tools/`(링크체커·YouTube API — PHASE 5) · 코워크 위임 = `10_system/cowork_prompts/`.

## 변경 이력
| 날짜 | 변경 |
|---|---|
| 2026-07-06 | **v2 전면 재작성** (전면개편 PHASE 3) — 유튜브 제작 OS 진입점화: S0~S9 지도·registry 라우팅·에이전트 13 맵·편향 금지 원칙·링크체커 의무 신설. v1(2026-05-03~07-05) 이력은 git 히스토리 참조 |
| 2026-07-06 | PHASE 4~7 완료 반영 — 마이그레이션 배너 제거(트리 확정), 파일럿(011) G1·G2 실통과, README v2·운영매뉴얼 등재. 개편 전 과정 기록 = `90_archive/2026-07_전면개편/` |
